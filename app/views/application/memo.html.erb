<div class="d-flex align-items-center">
  <h3><%= @group.name %></h3>
</div>

<%#Groupの詳細を表示%>
<table class="table table-hover boards__table mt-3">
  <td><%= @group.name %></td>
  <td><%= @group.introduction %></td>
  <td><%= link_to '編集', edit_group_path(@group), class: 'btn btn-outline-info'%></td> <%#将来的に幹事のみ実行に変更%>
  <td><%= button_to '削除', group_path(@group), class: 'btn btn-outline-danger', method: :delete %></td> <%#ここも%>
</table>

<%# ここにメモを記述 %>
<a class="nav-link"  data-method="delete" href="<%= login_path %>">ログアウト</a> 

<%# グループ参加申請の時にこの処理を記述 %>
<% if logged_in? %>
  <%= render 'groups/logged_in' %>
<% else %>
  <%= render 'groups/not_logged_in' %>
<% end %>

<%# バッックアッップ %>
<% (1..5).each do |i| %>
<% end %>

<%# テンプレ %>
docker-compose run web bundle exec rails g model <model(中)> <model(多)1>:references <model(多)2>:references
docker-compose run web bundle exec rails g model user_group_relation user:references group:references
<%# テンプレ %>
AddColumnNullToGroups